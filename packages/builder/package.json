{
  "name": "@proappstore/builder",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "PAS centralized build container — clone → vite build → R2 (ADR-006).",
  "scripts": {
    "build": "echo 'no build step (plain ESM container script)'",
    "typecheck": "echo 'no typecheck (plain ESM; tested via lib.test.ts)'",
    "test": "echo 'tests run from the workspace root vitest'"
  }
}
