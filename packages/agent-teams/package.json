{
  "name": "@proappstore/agent-teams",
  "version": "0.1.0",
  "private": true,
  "description": "PAS Agent Teams — Linear-for-AI-agents inside PAS. Backlog DO + supervision UI + pluggable AgentRuntime adapters (CFNative, OpenAI Responses). See ~/.gstack/projects/serge-ivo-stores-workspace/serge-ivo-main-design-20260521-181709.md.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit && esbuild src/index.ts --bundle --format=esm --target=es2022 --outfile=dist/worker.js --minify",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "hono": "^4.12.27"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250101.0",
    "esbuild": "^0.28.0",
    "typescript": "^5.7.0",
    "vitest": "^2.1.9",
    "wrangler": "^4.0.0"
  }
}
