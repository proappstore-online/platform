{
  "name": "@proappstore/backend",
  "version": "0.0.0",
  "description": "Cloudflare Worker — Stripe webhooks, subscriptions, license keys, app + listing CRUD, analytics, BYO custom domains.",
  "license": "MIT",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "embed-data-worker": "node scripts/embed-data-worker.mjs",
    "prebuild": "node scripts/embed-data-worker.mjs",
    "pretypecheck": "node scripts/embed-data-worker.mjs",
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "@proappstore/build-core": "workspace:*",
    "@proappstore/compliance": "workspace:*",
    "@proappstore/qa-spec": "workspace:*",
    "chess.js": "^1.4.0",
    "hono": "^4.12.27",
    "web-push": "^3.6.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250101.0",
    "@types/web-push": "^3.6.0",
    "typescript": "^5.7.0",
    "wrangler": "^4.0.0"
  }
}