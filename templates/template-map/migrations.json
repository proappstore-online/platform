{
  "migrations": [
    {
      "name": "0001_categories",
      "sql": "CREATE TABLE IF NOT EXISTS categories (id TEXT PRIMARY KEY, name TEXT NOT NULL, color TEXT NOT NULL DEFAULT '#d86f4d', icon TEXT NOT NULL DEFAULT '', sort_order INTEGER NOT NULL DEFAULT 0, created_at INTEGER NOT NULL, updated_at INTEGER NOT NULL)"
    },
    {
      "name": "0002_places",
      "sql": "CREATE TABLE IF NOT EXISTS places (id TEXT PRIMARY KEY, owner_id TEXT NOT NULL, owner_name TEXT NOT NULL DEFAULT '', category_id TEXT, name TEXT NOT NULL, description TEXT NOT NULL DEFAULT '', address TEXT NOT NULL DEFAULT '', lat REAL NOT NULL, lng REAL NOT NULL, status TEXT NOT NULL DEFAULT 'active', image_key TEXT, created_at INTEGER NOT NULL, updated_at INTEGER NOT NULL); CREATE INDEX IF NOT EXISTS idx_places_owner ON places (owner_id, updated_at DESC); CREATE INDEX IF NOT EXISTS idx_places_status_cat ON places (status, category_id); CREATE INDEX IF NOT EXISTS idx_places_lat_lng ON places (lat, lng)"
    }
  ]
}
